apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sporhund-purring-db-policy
  namespace: tbd
spec:
  egress:
    - to:
        - ipBlock:
            cidr: 100.71.4.133/32
  podSelector:
    matchLabels:
      db: sporhund
  policyTypes:
    - Egress

