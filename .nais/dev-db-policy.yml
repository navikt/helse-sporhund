apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sporhund-purring-db-policy
  namespace: tbd
spec:
  egress:
    - to:
        - ipBlock:
            cidr: 34.88.143.189/32
  podSelector:
    matchLabels:
      dbPolicyUser: purring
  policyTypes:
    - Egress

